# Insightful — Full stack (aggregate)
# Includes all stacks via 'include:' directives. Same as ./start.sh all.
# Usage: podman compose -f compose/insightful.yml up -d

name: insightful

include:
  - lifeos.yml
  - redis.yml
  - observability.yml
  - odysseus.yml
  - n8n.yml
  - qdrant.yml
  - npm.yml
  - ollama.yml
  - code-server.yml
  - insightful-hub.yml
  - codespace.yml
  - cloudbeaver.yml
  - oauth2.yml
